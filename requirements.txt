# Tested on Windows 11, Python 3.12, NVIDIA RTX 3050 (6 GB), CUDA 12.1.
# Install (see README "Setup"):
#   pip install torch==2.5.1 torchaudio==2.5.1 --index-url https://download.pytorch.org/whl/cu121
#   pip install -r requirements.txt
#   pip install --no-deps asteroid==0.7.0        # its pinned deps conflict with modern torch; the model code needs none of them
#   pip install --no-deps onnxruntime-gpu==1.19.2  # GPU ASR; newer builds need CUDA 13 or are ~15x slower with torch cu121
numpy==2.2.6
scipy==1.16.2
scikit-learn==1.7.2
pandas==2.3.2
pyarrow==25.0.0
soundfile==0.13.1
librosa==0.11.0
pyannote.audio==3.3.2
speechbrain==1.1.1
asteroid-filterbanks==0.4.0
transformers==4.57.6
huggingface_hub==0.36.2
hf_xet==1.6.0
jiwer==4.0.0
matplotlib==3.10.6
psutil==7.2.2
nbformat==5.11.1
nbconvert==7.17.1
ipykernel==7.3.0
pytest==9.1.1
tabulate==0.9.0
